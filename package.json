{
  "name": "owasp-security-validation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:security": "cucumber-js --require-module ts-node/register --require tests/step-definitions/**/*.ts --require tests/support/**/*.ts tests/features/security-validation.feature --format progress --format html:report.html"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "playwright": "^1.46.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
